<!DOCTYPE html>
<html>
<head>
    {{> header}}
    <title>Articles</title>
</head>
<body>
  <div class="page-container">
    <div class="content-wrap">
      <div class="container">
        {{> navbar}}<br>
        <div class="list-group">
            {{#articles}}
            <div class="d-flex w-100 justify-content-between">
              <h5 class="mb-1">{{title}}</h5>
            </div>
            <p class="mb-1">{{body}}</p>
            {{/articles}}
        </div>
        <br><br><br>
        {{#message}}
        <div class="alert alert-warning" role="alert">
          <a href="/login">Login</a> to add your own articles!
        </div>
        {{/message}}
      </div>
    </div>
    {{> footer}}
  </div>
</body>
</html>